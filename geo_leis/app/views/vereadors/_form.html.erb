<%= form_for(@vereador) do |f| %>
  <% if @vereador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vereador.errors.count, "error") %> prohibited this vereador from being saved:</h2>

      <ul>
      <% @vereador.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
